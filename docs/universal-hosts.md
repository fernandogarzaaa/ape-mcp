# Universal hosts

One binary, four manifests: `.claude-plugin/` (Claude), `.codex/skills/ape/` (Codex), `.opencode/plugin.json` (OpenCode), `mcpServers.json` (Cursor/Windsurf/VSCode/Desktop). All launch `bin/ape-mcp.js` (stdio) or `--http` (Streamable). Skill: `skills/ape/SKILL.md`.
