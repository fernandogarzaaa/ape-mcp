# Universal hosts

One binary, four manifests: `.claude-plugin/` (Claude), `.codex/skills/godmode/` (Codex), `.opencode/plugin.json` (OpenCode), `mcpServers.json` (Cursor/Windsurf/VSCode/Desktop). All launch `bin/godmode-mcp.js` (stdio) or `--http` (Streamable). Skill: `skills/godmode/SKILL.md`.
