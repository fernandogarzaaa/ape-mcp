# Closed-loop experiments

`typhoon_manila_closed_loop.yaml` is the first-domain closed loop:

WorldState(t0) → MiroFish-shaped sim → EVE → score vs WorldState(t1).

Old historical-replay YAML under `../historical-replay/` still loads via
`load_scenario`.
